version: "3.8"

services:
  backend:
    build:
      context: "./server"
      dockerfile: Dockerfile
    container_name: arcgis_oauth2_api
    restart: unless-stopped
    # ports:
    #   - "3080:3080"
    env_file:
      - "./server/.env"
    networks:
      - webnet

  # ui:
  #   build:
  #     context: "./client"
  #     dockerfile: Dockerfile
  #   container_name: arcgis_oauth2_client
  #   restart: unless-stopped
  #   ports:
  #     - "3080:3080"
  #   env_file:
  #     - "./server/.env"

  # simpleui:
  #   build:
  #     context: "./simple_ui"
  #     dockerfile: Dockerfile
  #   container_name: simpleui
  #   restart: unless-stopped
  #   ports:
  #     - "5173:5173"

  nginx:
    # image: nginx:latest
    build:
      context: ./simple_ui
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - backend
    networks:
      - webnet

networks:
  webnet:
    driver: bridge